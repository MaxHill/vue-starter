describe('Something can be tested', () => {
    it('should set correct default data', () => {
        expect(true).toBe(true);
    });

    it('should fail', () => {
        expect(false).toBe(false);
    });
});
